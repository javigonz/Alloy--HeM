// ANDROID *********************************************************

".viewAgenda[platform=android]": { 
	backgroundColor:Alloy.CFG.WHITE,
	top: 0
}

".containerAgenda[platform=android]": {
    width: Ti.UI.FILL,
    backgroundColor: Alloy.CFG.WHITE,
    layout: 'vertical',
    top: 0
}

".scrollableAgendaSlider[platform=android]": {	     
	separatorColor: Alloy.CFG.GREY_LIGHT,
    top: 0
}

".rowList[platform=android]": {
	height: Ti.UI.SIZE,
	backgroundColor: Alloy.CFG.WHITE,
	focusable: "true",
	left: 0,
	selectedBackgroundColor: Alloy.CFG.RED1
}

".rowLine[platform=android]": {
	height: 1,
	width: Ti.UI.FILL,
	backgroundColor: Alloy.CFG.ORANGE3,
	bottom: 1
}

".viewImage[platform=android]": {
	height: Ti.UI.SIZE,
	width: 120,
	left: 0,
	top: 10,
	bottom: 10	
}

".productImage[platform=android]": {
	left: 0
}

".viewDescription[platform=android]": {
	height: Ti.UI.SIZE,
	top: 10,
	bottom: 10,
	left: 130,
	width: 200,
	layout: 'vertical'

}

".textName[platform=android]": {
	color: Alloy.CFG.BLACK,
	font: {
		fontFamily: Alloy.CFG.GENEVA, 
		fontSize: 	13,
		fontWeight: 'bold'
	},
	left: 0
}

".icon[platform=android]": {
	left: 0,
	
}

".iconView[platform=android]": {
	top: 0,
	height: 20,
	layout: 'horizontal',
	
}

".textDate[platform=android]": {
	color: Alloy.CFG.ORANGE3,
	font: {
		fontFamily: Alloy.CFG.GENEVA, 
		fontSize: 	10,
		fontWeight: 'bold'
	},
	left: 3
}

".textDescription[platform=android]": {
	height: Ti.UI.SIZE,
	color: Alloy.CFG.GREY,
	font: {
		fontFamily: Alloy.CFG.GENEVA, 
		fontSize: 	11,
		fontWeight: 'normal'
	},
	left: 0,
	
	
}

".iconArrow[platform=android]": {
	right: 5
}

".viewTitle[platform=android]": {
	
	width: Ti.UI.FILL,
	
	backgroundColor: Alloy.CFG.ORANGE3,
	
}

".textTitle[platform=android]": {
	color: Alloy.CFG.WHITE,
	font: {
		fontFamily: Alloy.CFG.GENEVA, 
		fontSize: 	14,
		fontWeight: 'normal'
	}
}



// IPHONE *********************************************************

".viewAgenda[platform=ios]": { 
	backgroundColor:Alloy.CFG.WHITE,
	top: 0
}

".containerAgenda[platform=ios]": {
    width: Ti.UI.FILL,
    backgroundColor: Alloy.CFG.WHITE,
    layout: 'vertical',
    top: 0
}

".scrollableAgendaSlider[platform=ios]": {	     
	separatorColor: Alloy.CFG.GREY_LIGHT,
    top: 0
}

".rowList[platform=ios]": {
	height: Ti.UI.SIZE,
	backgroundColor: Alloy.CFG.WHITE,
	focusable: "true",
	left: 0,
	selectedBackgroundColor: Alloy.CFG.BLUE1
}

".rowLine[platform=ios]": {
	height: 1,
	width: Ti.UI.FILL,
	backgroundColor: Alloy.CFG.ORANGE3,
	bottom: 1
}

".viewImage[platform=ios]": {
	height: Ti.UI.SIZE,
	width: 120,
	left: 0,
	top: 10,
	bottom: 10	
}

".productImage[platform=ios]": {
	left: 0
}

".viewDescription[platform=ios]": {
	height: Ti.UI.SIZE,
	top: 10,
	bottom: 10,
	left: 130,
	width: 160,
	layout: 'vertical'

}

".textName[platform=ios]": {
	color: Alloy.CFG.BLACK,
	font: {
		fontFamily: Alloy.CFG.GENEVA, 
		fontSize: 	13,
		fontWeight: 'bold'
	},
	left: 0
}

".icon[platform=ios]": {
	left: 0,
	
}

".iconView[platform=ios]": {
	top: 0,
	height: 20,
	layout: 'horizontal',
	
}

".textDate[platform=ios]": {
	color: Alloy.CFG.ORANGE3,
	font: {
		fontFamily: Alloy.CFG.GENEVA, 
		fontSize: 	10,
		fontWeight: 'bold'
	},
	left: 3
}

".textDescription[platform=ios]": {
	height: Ti.UI.SIZE,
	color: Alloy.CFG.GREY,
	font: {
		fontFamily: Alloy.CFG.GENEVA, 
		fontSize: 	11,
		fontWeight: 'normal'
	},
	left: 0,
	
	
}

".iconArrow[platform=ios]": {
	right: 5
}

".viewTitle[platform=ios]": {
	
	width: Ti.UI.FILL,
	
	backgroundColor: Alloy.CFG.ORANGE3,
	
}

".textTitle[platform=ios]": {
	color: Alloy.CFG.WHITE,
	font: {
		fontFamily: Alloy.CFG.GENEVA, 
		fontSize: 	14,
		fontWeight: 'normal'
	}
}
